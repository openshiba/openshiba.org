export const defaultTransitionSpeedMs = 250;
export const defaultTransitionSpeed = `${defaultTransitionSpeedMs * 0.001}s`;
export const defaultTransitionType = 'ease';
export const defaultTransition = `${defaultTransitionType} all ${defaultTransitionSpeed}`;

export default {
  defaultTransitionSpeed,
  defaultTransitionSpeedMs,
  defaultTransitionType,
  defaultTransition,
}